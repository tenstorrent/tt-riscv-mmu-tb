# Auto-generated by regress/runreg.py — do not hand-edit.
# Regenerate: regress/runreg.py [options]
#
# PORTABLE: commands use "$MMU_TB_ROOT" instead of a hardcoded path, so
# this file works in any user's checkout. runreg.py exports MMU_TB_ROOT
# automatically (the repo containing runreg.py). If you run these commands
# by hand or through another launcher, set it first:
#   export MMU_TB_ROOT=/path/to/tt-riscv-mmu-tb

config:
  project: mmu
  owners: mmu-tb

build_mmu_tb_vcs:
  command: "make -C \"$MMU_TB_ROOT\" compile SIM=vcs"

test_vcs_mmu_ifu_test_s1:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_ifu_test PT_SEED=1 ARGS=\"+PT_SEED=1 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_ifu_test_s2:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_ifu_test PT_SEED=2 ARGS=\"+PT_SEED=2 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_ifu_test_s3:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_ifu_test PT_SEED=3 ARGS=\"+PT_SEED=3 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_lsu_test_s1:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_lsu_test PT_SEED=1 ARGS=\"+PT_SEED=1 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_lsu_test_s2:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_lsu_test PT_SEED=2 ARGS=\"+PT_SEED=2 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_lsu_test_s3:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_lsu_test PT_SEED=3 ARGS=\"+PT_SEED=3 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_ifu_lsu_test_s1:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_ifu_lsu_test PT_SEED=1 ARGS=\"+PT_SEED=1 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_ifu_lsu_test_s2:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_ifu_lsu_test PT_SEED=2 ARGS=\"+PT_SEED=2 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_ifu_lsu_test_s3:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_ifu_lsu_test PT_SEED=3 ARGS=\"+PT_SEED=3 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_tlb_inv_test_s1:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_tlb_inv_test PT_SEED=1 ARGS=\"+PT_SEED=1 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_tlb_inv_test_s2:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_tlb_inv_test PT_SEED=2 ARGS=\"+PT_SEED=2 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

test_vcs_mmu_tlb_inv_test_s3:
  command: "make -C \"$MMU_TB_ROOT\" run-only SIM=vcs TEST=mmu_tlb_inv_test PT_SEED=3 ARGS=\"+PT_SEED=3 +PT_DYNAMIC\""
  deps: [build_mmu_tb_vcs]

